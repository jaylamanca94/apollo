# Apollo — Design Status

**Last reviewed:** 2026-08-11
**Canonical sources:** `FLOW-REGISTRY.md`, `PRODUCT-README.md`, `DESIGN-README.md`, `docs/product-lab-progress.md`, `product-review-2026-08-10/PRODUCT-REVIEW.md`, `product-review-2026-08-11/PRODUCT-REVIEW.md`

## Active-flow headline

Apollo's dashboard is the clearest expression of its purpose—plain-English understanding of space activity now—and its partial state now has accepted controlled 390px and 320px evidence; linked Asteroids recovery, ISS-and-crew local-function loaded/recovery, Launches overview/disclosure/recovery, Weather decision/source/recovery, mobile Watch keyboard navigation, and Sky Anomalies keyboard submission also extend beyond desktop proof.

## Signals

| Signal | Verified status |
| --- | --- |
| Canonical flows | 8 distinct user-goal flows retained; 5 core, 2 supporting, 1 experimental. Weather and Asteroids are tracked separately because they answer different user questions. |
| Recovery coverage | ISS/Crew, Launches, Weather, Asteroids, and Gallery show source-specific unavailable states; each affected detail page offers an in-context retry plus source and Dashboard routes. ISS position and crew now use Apollo’s own cache-backed source routes, giving their user-facing recovery the same boundary as the other detail flows. The Dashboard also sends non-loaded source cards to the relevant Apollo detail page before offering the separate upstream link; the NeoWs route and retry are accepted in the current Vercel preview |
| Design-system alignment | The recovery control reuses Acadia button and state anatomy; Apollo-specific red identity and source semantics remain local |
| Incomplete flows | The Dashboard has accepted Vercel-runtime desktop plus controlled 390px and 320px partial-state captures, Space Refresh, and a pointer route from unavailable APOD/NeoWs source cards into Apollo recovery; Asteroids’ 390px unavailable-recovery actions clear the fixed dock at the true scroll limit and now complete an Enter retry. ISS/Crew has accepted desktop plus local-function-preview 390px loaded and controlled simultaneous source-failure captures, including map/orbital context, complete roster, source count, pointer Refresh, and two explicit no-stale-data recovery cards. Launches and Weather each have current controlled 390px loaded, source-action, and API-free unavailable-recovery coverage; at 320px Launches, Weather, Asteroids, and Gallery all have no-overflow unavailable states with every recovery action clearing the dock. Gallery and Asteroids retain only desktop loaded-state proof. The mobile Watch menu now has controlled browser Enter/Space opening, first-item focus, Weather/Asteroids/Sky Anomalies destinations, and dismissal coverage. Sky Anomalies has accepted controlled browser radio selection and Enter submission at 390px, preserving traits and focusing the result below the sticky header. All flows still need complete keyboard, broader mobile/reflow, screen-reader, and deployed-production QA. |
| Missing states | No adopted aged-data/last-known-good state; source outages stay honest rather than appearing live. Sky Anomalies has explicit planned-source gaps rather than fabricated matching, and its submitted explanation now reflects the selected observation duration. |
| Evidence limit | `npm run dev` normally creates the temporary copied checkout needed because Vercel's worker cannot interpret the CloudDocs path with spaces. Its earlier runtime confirmed the Dashboard partial state plus ISS/Crew, Launches, Weather, Gallery, Asteroids, and Sky Anomalies, but is not deployed-production proof. The current invocation cannot start because the configured Vercel token is invalid. The new authentication-free `npm run preview` validates the ISS/Crew proxy routes against live public sources, but is deliberately not Vercel-platform evidence. NASA's public, rate-limited `DEMO_KEY` was supplied only to the local process for the latter two flows and was not saved; a current ephemeral local use still returned Apollo's unavailable NeoWs state, so a dedicated non-production key is required for repeatable Asteroids loaded-state QA. The Dashboard partial-state capture intentionally had no NASA key, so it does not substitute for full-live Dashboard evidence. Current controlled 390 × 844 and 320 × 844 CSS viewports reported matching `innerWidth`, `clientWidth`, and `scrollWidth`; Dashboard partial at both widths; Launches loaded overview/disclosure/source action and controlled static-shell recovery at 390px plus no-overflow 320px recovery; Weather loaded decision/source context and controlled recovery at 390px plus no-overflow 320px recovery; ISS/Crew local-function loaded and controlled simultaneous source recovery; Asteroids and Gallery unavailable recovery at both 390px and 320px; Sky Anomalies start/submitted-result; and the complete Watch pointer-destination states are accepted, while full mobile coverage and hardware checks remain open. At 320px, unavailable-state actions end at 682px for Launches, 718px for Weather, 714px for Asteroids, and 665px for Gallery while the dock begins at 772px. At 320px, submitted Sky Anomalies focus lands on `Sighting context` 86px below the sticky header. The local function preview now confirms Watch keyboard operation, Sky Anomalies radio/submit activation, and Asteroids’ source-specific Enter retry. Unavailable-state screenshot output that did not match the visible DOM remains rejected as visual evidence. |

## Highest-leverage design opportunities

1. **P1 — Decide an explicit resilience policy for unavailable sources.** Either retain validated, visibly aged data with a maximum age or commit to the current no-stale-data experience; this affects the Dashboard, Launches, Weather, Asteroids, and Gallery flows.
2. **P1 — Make the Dashboard's Space Brief the unambiguous primary moment.** Resolve whether it is a concise decision surface or a broad feed browser, then remove secondary structure that does not serve that choice; this affects the Dashboard, ISS, Launches, Weather, Asteroids, and Gallery flows.
3. **P2 — Keep Sky Anomalies deliberately bounded until its evidence improves.** Lower its prominence or define a focused pilot only after a small location-aware source set is ready; this affects the Anomalies flow and the Dashboard/Watch entry paths.
