# Apollo — Design Status

**Last reviewed:** 2026-08-11
**Canonical sources:** `FLOW-REGISTRY.md`, `PRODUCT-README.md`, `DESIGN-README.md`, `docs/product-lab-progress.md`, `product-review-2026-08-10/PRODUCT-REVIEW.md`, `product-review-2026-08-11/PRODUCT-REVIEW.md`

## Active-flow headline

Apollo's dashboard is the clearest expression of its purpose—plain-English understanding of space activity now—and its partial state now has accepted controlled 390px and 320px evidence; linked Asteroids recovery, ISS-and-crew loaded state, Launches overview/disclosure/recovery, and Weather decision/source context also extend beyond desktop proof. Sky Anomalies now hands a submitted sighting directly to its visible, focused result.

## Signals

| Signal | Verified status |
| --- | --- |
| Canonical flows | 8 distinct user-goal flows retained; 5 core, 2 supporting, 1 experimental. Weather and Asteroids are tracked separately because they answer different user questions. |
| Recovery coverage | ISS/Crew, Launches, Weather, Asteroids, and Gallery show source-specific unavailable states; each affected detail page offers an in-context retry plus source and Dashboard routes. The Dashboard now also sends non-loaded source cards to the relevant Apollo detail page before offering the separate upstream link; the NeoWs route and retry are accepted in the current Vercel preview |
| Design-system alignment | The recovery control reuses Acadia button and state anatomy; Apollo-specific red identity and source semantics remain local |
| Incomplete flows | The Dashboard has accepted Vercel-runtime desktop plus controlled 390px and 320px partial-state captures, Enter refresh, and a pointer route from unavailable APOD/NeoWs source cards into Apollo recovery; Asteroids’ 390px unavailable-recovery actions clear the fixed dock at the true scroll limit. ISS/Crew has accepted desktop and controlled 390px loaded-state captures, including its map, orbital context, complete roster, and source count; its mobile source-failure state remains open. Launches has current controlled 390px loaded overview, disclosure, source-action, and API-free unavailable-recovery coverage; its 160px phone image keeps Liftoff above the dock, but its keyboard, hardware, and production coverage remain open. Weather has current controlled 390px loaded decision and lower source-context coverage; its unavailable mobile, keyboard, hardware, and production coverage remain open. Gallery and Asteroids have accepted Vercel-runtime desktop loaded-state captures; Gallery also has an accepted controlled 390px unavailable-recovery state. The mobile Watch menu has accepted pointer coverage to Weather, Asteroids, and Sky Anomalies with destination dismissal. Gallery and Asteroids also completed pointer and Enter refresh while loaded. Asteroids’ unavailable retry also has accepted visible focus evidence. Sky Anomalies has accepted desktop, 390px, and 320px start/submitted-result captures, including source limits and a focused result handoff below the sticky header, plus native-form semantics and focus-handoff regression checks. All flows still need complete keyboard, broader mobile/reflow, screen-reader, and deployed-production QA. |
| Missing states | No adopted aged-data/last-known-good state; source outages stay honest rather than appearing live. Sky Anomalies has explicit planned-source gaps rather than fabricated matching, and its submitted explanation now reflects the selected observation duration. |
| Evidence limit | `npm run dev` now creates the temporary copied checkout needed because Vercel's worker cannot interpret the CloudDocs path with spaces. That runtime confirmed the Dashboard partial state plus ISS/Crew, Launches, Weather, Gallery, Asteroids, and Sky Anomalies, but is not deployed-production proof. NASA's public, rate-limited `DEMO_KEY` was supplied only to the local process for the latter two flows and was not saved; a current ephemeral local use still returned Apollo's unavailable NeoWs state, so a dedicated non-production key is required for repeatable Asteroids loaded-state QA. The Dashboard partial-state capture intentionally had no NASA key, so it does not substitute for full-live Dashboard evidence. Current controlled 390 × 844 and 320 × 844 CSS viewports reported matching `innerWidth`, `clientWidth`, and `scrollWidth`; Dashboard partial at both widths, Launches loaded overview/disclosure/source action, controlled static-shell Launches recovery, Weather loaded decision/source context, Asteroids unavailable-recovery, Gallery unavailable-recovery, Sky Anomalies start/submitted-result, ISS/Crew loaded, and the complete Watch pointer-destination states are accepted, while full mobile coverage and hardware checks remain open. The static shell's unavailable Launches actions end at 636px while the dock begins at 772px. At 320px, submitted Sky Anomalies focus lands on `Sighting context` 86px below the sticky header. Native mobile Watch keyboard activation remains unproven because the current controller did not open it with Enter or Space. The same controller focused Sky Anomalies radios and submit without activation, and focused Asteroids' retry without dispatching Enter or Space through its CUA, DOM, and direct key channels. Focus-ring evidence is accepted; no key-activation claim is made. Unavailable-state screenshot output that did not match the visible DOM remains rejected as visual evidence. |

## Highest-leverage design opportunities

1. **P1 — Decide an explicit resilience policy for unavailable sources.** Either retain validated, visibly aged data with a maximum age or commit to the current no-stale-data experience; this affects the Dashboard, Launches, Weather, Asteroids, and Gallery flows.
2. **P1 — Make the Dashboard's Space Brief the unambiguous primary moment.** Resolve whether it is a concise decision surface or a broad feed browser, then remove secondary structure that does not serve that choice; this affects the Dashboard, ISS, Launches, Weather, Asteroids, and Gallery flows.
3. **P2 — Keep Sky Anomalies deliberately bounded until its evidence improves.** Lower its prominence or define a focused pilot only after a small location-aware source set is ready; this affects the Anomalies flow and the Dashboard/Watch entry paths.
