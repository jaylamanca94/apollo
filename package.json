{
  "name": "apollo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/api-contracts.test.js",
    "check": "node --check app.js && node --check api/_nasa.js && node --check api/_space_data.js && node --check api/apod.js && node --check api/neo.js && node --check api/launches.js && node --check api/health.js && node --check tests/api-contracts.test.js && npm test"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
